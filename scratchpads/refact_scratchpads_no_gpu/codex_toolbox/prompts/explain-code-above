DO:
"""
Explain the code
"""
CODE:
"""
    def completion(self, final: bool, tokens_batch: Optional[int] = 25) -> Iterator[Dict[str, str]]:
        tokens_batch: int = self.max_tokens if final else tokens_batch

        return self.completion_stream(
            engine=self._engine,
            tokens_batch=tokens_batch,
            prompt=self.prompt,
            replace_modified=self._replace_modified
        )
"""
ANSWER:
"""
    def completion(self, final: bool, tokens_batch: Optional[int] = 25) -> Iterator[Dict[str, str]]:
        tokens_batch: int = self.max_tokens if final else tokens_batch # if not tokens_batch given, using max_tokens

        return self.completion_stream(
            # a model codify API uses. E.g.  text-davinci-003, code-davinci-002 etc
            engine=self._engine,
            # count of tokens that will be in each batch
            tokens_batch=tokens_batch,
            # function, that generates a prompt for GPT3, using context and examples
            prompt=self.prompt,
            # function, that places generated text to original file
            replace_modified=self._replace_modified
        )

DO:
"""
Explain the code
"""
CODE:
"""
cli = click.CommandCollection(sources=[ext_nodes_cli])
"""
ANSWER:
"""
# creates a new CommandCollection object with ext_nodes_cli as the source
cli = click.CommandCollection(sources=[ext_nodes_cli])

DO:
"""
Explain the code
"""
CODE:
"""
ext_nodes_cli.add_command(filter_repos, name='filter')
ext_nodes_cli.add_command(daemon, name='daemon')
ext_nodes_cli.add_command(tasks_setup, name='setup')
ext_nodes_cli.add_command(stats, name='stats')
"""
ANSWER:
"""
# adds a command 'filter' to the ext_nodes_cli
ext_nodes_cli.add_command(filter_repos, name='filter')
# adds a command 'daemon' to the ext_nodes_cli
ext_nodes_cli.add_command(daemon, name='daemon')
# adds a command 'setup' to the ext_nodes_cli
ext_nodes_cli.add_command(tasks_setup, name='setup')
# adds a command 'stats' to the ext_nodes_cli
ext_nodes_cli.add_command(stats, name='stats')

DO:
"""
Explain the code
"""
CODE:
"""
{___place_code_here___}
"""
ANSWER:
"""
